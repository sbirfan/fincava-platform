node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.vite/
coverage/

# Replit Agent's own auto-generated folder for chat-attached files (grows
# over time, not developer-curated content). Real brand assets we actually
# use are copied into client/public/ instead — see README "Tracked TODOs".
attached_assets/
