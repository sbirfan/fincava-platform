// Phase 0 placeholder — Home, Available Lots, Lot Passport, About, Contact,
// Verification, auth, and admin pages land in Phases 1-4 per the execution
// spec's phased plan. This confirms the build pipeline boots end to end.
export default function App() {
  return (
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>FINCAVA Platform</h1>
      <p>Green Coffee Buyer Relationship Platform — under construction (Phase 0).</p>
    </div>
  );
}
